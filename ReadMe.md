# QtPlayer
A video player base on Qt + FFmpeg.

Render images using OpenGL, Play auido with QAudioSink.

![qtplayer.png](qtplayer.png)

# Dependencies
- Qt 6.6.3 (OpenGL may have issue in other versions)
- FFmpeg 6